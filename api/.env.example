# Copy to `api/.env` for local development (never commit real secrets).
# For Gmail, use an App Password (recommended) instead of your normal password.
GMAIL_USER=your.email@gmail.com
GMAIL_PASS=your_gmail_app_password
# Optional: where to receive contact form notifications (defaults to GMAIL_USER)
NOTIFY_TO=you@example.com
